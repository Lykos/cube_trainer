// Note that this is stored in the database, so we can't just change around values.
export enum StatTypeId {
  Averages = 'averages',
  SuccessAverages = 'success_averages',
  SuccessRates = 'success_rates',
  Mo3 = 'mo3',
  Progress = 'progress',
}
